qemu-system-i386 -cdrom os.iso
                 - hda fs.img

                 hda : premier disque dur du premier controleur


Taille minimum d'un block definissable pas l'user ?
file entries : 10000 files entries pour chaque fichier, indique le numero des blocks qui contiennent le contenu du fichier
File name, string de 32bytes max, 0 inclu
Des que l'on a un index avec la valeur 0, c'est la fin du fichier
Si premier character de la file entries = 0, file entries pas utilise
fin de la file entries ?
data blocks : contenu des fichiers 0 ou plusieurs blocks de donnees par fichier

bitmap : si fichier pas utilise, 0, si utilise 1
maping 1:1
1 bits du bitmap = 1er block de donnees


creation d'un fichier :
localiste filentri qui commence a 0
check le nom du fichier, indiquer sa taille, trouver X blocks pour remplir le fichier et indiquer
les indices de chaque block a la suite, avec un 0 pour la fin du fichier,
diviser le contenu du fichier en block et ecrire dans les blocks
suppression d'un fichier, supprime seulement le fileentry


tools : pfscreate, pfsadd, sfslist, sfsdel, ...

